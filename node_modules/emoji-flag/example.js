var flag = require('./')
var countries = require('country-list/data')

var country = process.argv[2]
var loose = RegExp('^' + country + '$', 'i')
console.log(flag(countries.filter(function (c) {
  return loose.test(c.name)
})[0].code))
